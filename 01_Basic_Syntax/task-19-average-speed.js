function calculateAverageSpeed(distance, time) {
    let averageSpeed = distance / time
    console.log(averageSpeed);
}

calculateAverageSpeed(200, 2);
calculateAverageSpeed(150, 3);
calculateAverageSpeed(300, 5);